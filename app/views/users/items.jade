//#user(data-id=user._id.toString()) User: #{user.username}

#items
    each item in user.items
        case item.type
            when 'autogrow'
                include ../items/autogrow
            when 'autoseed'
                include ../items/autoseed
            when 'autoroot'
                include ../items/autoroot

#slider-connect

//- block scripts
//-     script(src='jquery.nouislider.js')
